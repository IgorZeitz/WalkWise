import pickle
import scipy.io
import numpy as np
import matplotlib.pyplot as plt
import cv2

pickle_file = 'X_9_RGB.pickle'  #file source

with open(pickle_file, 'rb') as f:
    data = pickle.load(f)

print(f'Typ danych: {type(data)}')
print(f'Kształt danych: {np.array(data).shape}')

#
heatmaps = np.array(data)


# save to matlab
scipy.io.savemat('heatmapy.mat', {'heatmaps': heatmaps})

print("Dane zapisane jako heatmapy.mat")


#
with open('X_9_RGB.pickle', 'rb') as f:
    data = pickle.load(f)

heatmaps = np.array([cv2.resize(img, (16,16), interpolation=cv2.INTER_AREA) for img in data])#np.array(data)  # shape: (n, h, w, 3)

# if RGB
plt.imshow(heatmaps[50].astype(np.uint8))
plt.title("RGB Heatmap")
plt.axis('off')
plt.show()